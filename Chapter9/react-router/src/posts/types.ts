export type PostData = {
  id: number;
  title: string;
  description: string;
};

export type NewPostData = {
  title: string;
  description: string;
};

export type SavedPostData = {
  id: number;
};
